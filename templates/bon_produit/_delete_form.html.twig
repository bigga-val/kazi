<form method="post" action="{{ path('app_bon_produit_delete', {'id': bon_produit.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce bon ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bon_produit.id) }}">
    <button class="btn btn-outline-danger">Delete</button>
</form>
