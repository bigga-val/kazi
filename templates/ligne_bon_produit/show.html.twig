<!DOCTYPE html>

<title>LigneBonProduit</title>

{% block body %}
    <h1>LigneBonProduit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ligne_bon_produit.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ligne_bon_produit_index') }}">back to list</a>

    <a href="{{ path('app_ligne_bon_produit_edit', {'id': ligne_bon_produit.id}) }}">edit</a>

    {{ include('ligne_bon_produit/_delete_form.html.twig') }}
{% endblock %}
