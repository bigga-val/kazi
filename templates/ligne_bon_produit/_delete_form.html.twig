<form method="post" action="{{ path('app_ligne_bon_produit_delete', {'id': ligne_bon_produit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ligne_bon_produit.id) }}">
    <button class="btn">Delete</button>
</form>
